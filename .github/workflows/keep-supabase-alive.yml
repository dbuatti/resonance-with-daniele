name: Keep Supabase Alive

on:
  schedule:
    - cron: "0 12 */3 * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase project
        run: |
          curl -s -o /dev/null -w "%{http_code}\n" --connect-timeout 10 \
            "https://xefifvztgeiuvzibiltj.supabase.co"
